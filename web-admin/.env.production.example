# 운영(production) 환경 변수 템플릿
# 1. 이 파일을 .env.production 으로 복사
# 2. NEXT_PUBLIC_API_BASE 를 운영 백엔드 도메인으로 교체
# 3. npm run build && npm run start 로 운영 모드 실행
# 4. (선택) 정적 호스팅 (Vercel, Netlify, Cloudflare Pages) 으로 배포

NEXT_PUBLIC_API_BASE=https://api.example.com
